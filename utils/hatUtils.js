export const getHatImageSource = (hatId) => {
    console.log(`Attempting to load hat image (OLD) for: ${hatId}`)
    // Placeholder implementation - replace with actual logic if needed
    return null
  }
  